version: 1.0
runtime: custom
build:
  commands:
    build:
      - curl https://sh.rustup.rs -sSf | sh -s -- -y
      - source $HOME/.cargo/env
      - cargo build --release
run:
  command: ./target/release/depplay-backend
  network:
    port: 8080